#ifndef MENU_H
#define MENU_H

#include "../utils.h"
#include "../const.h"

class Menu {
private:
  std::vector<std::string> layout;
  /* to be completed */

public:

  /* constructor */
  Menu();

  /* other methods */
  void run();
};

#endif