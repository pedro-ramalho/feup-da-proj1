#include "include/classes/Menu.h"

int main() {
  /* generates 10 assistant files and 10 delivery files */
  // for (int i = 5; i <= 15; i++)
  //   generate_data(i);
  Menu menu;
  menu.run();
  
  return 0;
}